// Twitter card image — reuses the Open Graph image implementation so both stay
// in sync. Next injects twitter:image tags via this file convention.
export { default, alt, size, contentType } from "./opengraph-image";
